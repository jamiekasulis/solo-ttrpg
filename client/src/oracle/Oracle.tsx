/**
 * Oracle includes a Table, Roll button, and a text box to show the result.
 */
import React, { FC } from 'react';

interface OracleProps {
    
}

const Oracle: FC<OracleProps> = ({

}) => {
    return (
        <>
            
        </>
    )
}

export default Oracle;